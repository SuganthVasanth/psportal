PORT=5000

# Google OAuth (Passport) — Authorized redirect URIs in Google Cloud Console (exact match, no trailing slash):
#   http://localhost:5000/auth/google/callback
#   http://127.0.0.1:5000/auth/google/callback   (optional, if you use 127.0.0.1 to reach the API)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

JWT_SECRET=
