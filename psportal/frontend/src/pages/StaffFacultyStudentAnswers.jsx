import React from "react";

export default function StaffFacultyStudentAnswers() {
  return (
    <section className="ud-card">
      <h3 className="card-title">Student answers</h3>
      <p className="card-subtitle">View student answers for your assigned courses (if access is granted by admin).</p>
      <p className="sa-muted">Student answers will appear here when admin enables this access for your role.</p>
    </section>
  );
}
